from django.apps import AppConfig

class SeguridadAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.seguridad_app'
    verbose_name = 'Seguridad y Auditoría'
